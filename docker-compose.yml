services:
  nginx:
    container_name: test-website-nginx
    image: nginx:latest
    ports:
      - 79:80
    volumes:
      - './:/usr/share/nginx/html'
    networks:
      - nginx_default
networks:
  nginx_default:
    external: true
